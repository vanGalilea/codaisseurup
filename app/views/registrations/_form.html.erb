<%= form_for [@event, @event.registrations.new] do |f| %>
  <p><strong>Price per person:</strong> <span>€</span> <%= @event.price %></p>

  <hr>

  <div class="form-group">
    <label>Check In:</label>
    <%= f.select :guests_count, [["1", 1], ["2", 2], ["3", 3]], prompt: "Select number of guests inc. you (max. 3)", class: "form-control" %>
  </div>

  <hr>

  <div class="actions">
    <%= f.submit "Register Now", class: "btn btn-success" %>
  </div>
<% end %>
